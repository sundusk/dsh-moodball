# MoodBall 后续版本开发计划（纳入多任务 UI 参考）

## 1. 开发顺序与范围

按照实用性、开发成本、现有能力和外部依赖排序：

| 顺序 | 功能 | 难度 | 安排理由 |
|---|---|---|---|
| 先行考察 | 点击任务打开完整会话 | 待核实 | 核实官方导航入口，不阻塞其他功能 |
| 第一阶段（已完成） | 全局快捷键＋Mini 模式 | 低至中 | 高频使用，主要修改原生界面 |
| 第二阶段 | 多任务卡片＋展开折叠＋未读提醒 | 中 | 减少切回 Harness，复用现有会话状态 |
| 第三阶段（已实现，待运行态验收） | 粘贴截图＋框选截图 | 中至高 | 实用性高，需要完善附件传输 |
| 第四阶段 | `@` 文件／目录＋`$` 技能选择 | 中 | 提高任务输入效率，复用官方服务 |
| 第五阶段 | 自定义宠物导入 | 中 | 个性化功能，可独立交付 |
| 暂缓 | 语音、Computer Use 画中画 | 高 | 等待官方配套能力成熟 |
| 移除 | 自动生成宠物 | — | 不进入开发范围 |

各阶段独立开发和验收。仅修改 MoodBall 应用及插件，官方 Harness 源码保持只读，保留现有无关改动。

## 2. 分阶段实现

### 先行考察：精确打开会话

限时检查官方公开深链、Web 导航或插件扩展点。

- 有可验证入口：纳入第二阶段。
- 没有可用入口：提供“打开 Harness”“复制会话 ID”，精确跳转暂缓。
- 不猜测 URL，不模拟 DOM 点击，不修改官方源码。
- 记录依据、验证结果和限制，不把“未发现入口”写成“官方不支持”。

### 第一阶段：快捷入口（已完成）

> 状态：已完成（2026-09-13）。第一阶段功能已实现、构建、安装并完成实际设置面板验证；代码已上传至 GitHub。

- 全局快捷键默认 `Option+Space`，支持修改或禁用；冲突时提示，不覆盖其他应用。
- 快捷键显示输入框并聚焦，重复按下保持打开，不清空草稿。
- 增加“宠物＋控件”“仅控件”两种模式；Mini 模式可独立拖动和保存位置。
- 隐藏全部与隐藏宠物形象分别处理。
- 保留中文输入、点击穿透、Esc 收起及草稿恢复行为。

### 第二阶段：多任务卡片与提醒

**以用户此次提供的截图作为布局与交互参考。开发时用户会再次提供原图，不依赖当前临时图片路径。**

采用截图中的“宠物 → 紧凑操作栏 → 任务卡片”纵向布局。宠物继续使用现有素材，不复刻截图中的宠物形象、背景或说明文字。

**操作栏与展开折叠**

- 保留输入按钮，加入任务入口／提醒指示和展开折叠箭头；不实现截图中的语音按钮，也不保留空白占位。
- 原“铃铛任务列表”采用本次截图的下方卡片呈现，不再另做重复的独立列表弹窗。
- 多任务默认折叠，显示一张主要任务卡；通过轻微堆叠和数量提示表达还有其他任务。
- 点击向下箭头展开，任务卡纵向排列；展开后箭头向上，再次点击折叠。
- 同一使用期间保留用户展开选择；应用重新启动后默认折叠。任务状态变化不强制展开。
- 没有任务时隐藏卡片和展开箭头；仅一个任务时显示单卡，不显示无意义的展开按钮。

**卡片内容与排序**

- 展示当前所选工作区的全部普通会话，包括网页端和宠物发起的任务；不单列内部子 Agent。
- 卡片包含标题、简短状态、右侧状态图标；详细时间放在展开后的详情中。
- 顺序为：等待操作、失败未读、完成未读、运行中、其他最近更新任务。折叠时显示排序第一项。
- 完成使用勾选图标，运行中使用进度指示，待操作和失败使用对应图标；不只靠颜色区分。
- 标题最多两行，过长截断；卡片高度随内容调整。
- 展开列表最多直接显示四张卡，超出后内部滚动；靠近屏幕边缘时缩小可用高度，不移动宠物锚点、不让卡片越界。

**查看与继续任务**

- 点击卡片查看该任务状态详情，并标记该项已读；展开列表不批量清除未读。
- “继续此会话”是独立操作，才改变输入目标；有草稿时先处理草稿，避免误带入其他任务。
- 提醒展示的任务与实际发送目标分开维护，输入框始终明确显示目标。
- 如已验证精确导航入口，详情提供“打开此会话”；否则提供打开 Harness 和复制 ID。
- 首次加载不把所有历史任务标记为未读；本机保存已读位置，重连后恢复。
- 编辑消息时暂时收起任务列表，保留展开选择；结束编辑后恢复，避免输入框与任务卡重叠。
- Mini 模式复用相同操作栏和任务卡布局。
- 本阶段不申请系统通知权限，不实现宠物内授权或回答表单。

### 第三阶段：截图附件

- 支持粘贴剪贴板图片，以及从输入框菜单启动框选截图。
- 框选时隐藏宠物和附属控件，完成或取消后恢复。
- 截图进入附件区，支持缩略图预览、移除和失败重试；不自动发送、不提取窗口文本。
- 支持图片单独发送或随文字发送；数量、大小遵循 Harness 附件限制。
- 使用官方附件持久化及消息接收机制，不把临时文件路径当作图片内容发送。
- 发送确认前保留草稿和图片；取消截图不修改草稿。
- 私有草稿文件在移除或发送确认后清理，不删除 Harness 已保存的附件。
- 按当前模型支持图像输入设计，准确显示服务端错误，不静默丢弃图片。

### 第四阶段：文件与技能

**`@` 文件／目录**

- 搜索目标工作区文件和目录，支持键盘选择、取消和引用移除。
- 复用官方候选服务及引用格式，正确处理空格、中文和目录。
- 文件读取交给 Harness，不自行上传整个目录。
- 暂不支持网页、其他应用或其他会话引用。

**`$` 技能选择**

- `$` 作为宠物端选择入口，列出目标会话可由用户调用的技能。
- 每条消息支持显式选择一个技能，可移除。
- 发送时转换为 Harness 识别的消息开头 `/技能名`，不直接发送 `$技能名`。
- 会话或工作区配置变化后更新目录。
- 查询需要时可提前创建空会话，但不触发模型运行。

### 第五阶段：自定义宠物导入

- 保留内置宠物，增加素材包导入、预览、选择、刷新和删除。
- 首版使用素材目录、清单文件和 PNG 图集，提供示例包及制作说明。
- 清单定义名称、帧尺寸、帧数、速度和状态动作；待机动作必需，其他动作缺失时使用待机。
- 校验路径、尺寸和资源完整性，不执行素材包代码。
- 刷新不改变位置；删除当前素材后恢复内置宠物。
- 不包含自动生成、在线市场或任意格式自动识别。

## 3. 技术接口与兼容

沿用 **Swift 原生界面 → 本地命令 Socket → 官方 Harness 服务**。

- 保留纯文字命令及旧状态接口，通过能力标识逐步增加功能；旧插件缺少能力时只禁用对应入口。
- 增加工作区会话摘要、多会话订阅、文件候选、技能目录和附件提交接口。
- 任务真实状态来自 Harness；已读位置、卡片展开状态、显示模式和草稿由 MoodBall 保存。
- 提醒任务、输入目标、工作区和异步请求分别关联，拒绝过期结果覆盖当前界面。
- 保留请求 ID 与重复提交保护，避免重试产生重复任务或消息。
- 不新增未经鉴权的 HTTP 写接口，不引入另一套 Agent 执行系统。
- 每阶段实施前核对官方接口变化，优先复用现有依赖和系统能力。

## 4. 验证与交付

每阶段交付代码、构建产物、使用说明及“已验证／未验证／阻塞”记录。

- **快捷入口**：跨应用唤起、快捷键冲突、中文输入、穿透、模式切换和重启恢复。
- **多任务 UI**：按用户截图检查折叠堆叠、展开排列、箭头切换、间距；覆盖零项、单项、多项、长标题、滚动、多屏及屏幕边缘。
- **任务正确性**：网页端任务可见，多任务不串状态，展开不误清未读，查看任务不改变发送目标，重连后恢复正确。
- **截图**：模型真实收到图片；取消、权限拒绝、上传失败、超限和断线不丢草稿、不重复发送。
- **文件与技能**：引用真实被识别，技能真实加载，不能以显示标签代替功能验收。
- **素材导入**：有效包可播放，损坏包被拒绝，刷新、删除、重启及减少动态效果正常。
- 运行现有 TypeScript 类型检查、构建、协议测试及 Swift 构建，补充针对性行为测试。
- 实际 UI 和 Harness 联调是最终验收条件；构建通过不能代替运行态验收。

不自动推送、发布或替换已安装应用。服务重载和实际安装在后续实施时按明确交付范围处理。

## 5. 第一阶段实施记录（2026-09-13）

### 已实现

- 增加基于 Carbon 的全局快捷键，默认为 `Option+Space`；支持在设置面板录入新组合键、禁用快捷键，并在注册冲突时保留上一次有效快捷键且提示用户。
- 快捷键会显示并聚焦原生输入框；重复触发不会收起输入框或清空草稿，原有 Esc、中文输入法候选确认和点击穿透行为保留。
- 增加“宠物＋控件”和“仅控件（Mini）”显示模式；“隐藏全部”与隐藏宠物形象分开处理。
- Mini 控件增加独立拖动把手，位置单独保存并在显示器变化时回收至可视区；仍遵守“记住拖拽位置”和“永远点击穿透”设置。
- README 已补充第一阶段使用说明。

### 已验证

- `swift build -c debug --disable-sandbox`
- `swift build -c release --disable-sandbox`
- `MOODBALL_SKIP_OPEN=1 bash make-app.sh`
- `codesign --verify --deep --strict --verbose=2 dist/MoodBall.app`
- `pnpm typecheck`、`pnpm test:protocol`：3 项协议测试通过
- `git diff --check`
- Release App 二进制可启动并保持菜单栏进程运行；已按单一安装策略保留 `/Applications/MoodBall.app`，其它安装副本和 `dist` 副本移入废纸篓。
- 设置面板宠物预览改为底部对齐并裁剪预览边界，修复小雨可见形象溢出到分段按钮区域的问题；已通过实际设置面板截图核验。
- 设置面板新增「快捷键」页（位于「行为」之后），集中管理全局唤起快捷键，以及输入消息、新建会话、选择工作区、打开 Harness、显示/隐藏浮层、设置和状态展示等功能的菜单快捷键；行为页保留网络、穿透与位置设置。

### 未验证 / 限制

- 当前环境的计算机控制层无法读取无窗口菜单栏 App 的 AX 面板，因此尚未完成跨应用真实快捷键唤起后的输入聚焦、快捷键冲突提示、Mini 鼠标拖动和重启恢复的人工 UI 验收。
- 未进行 Harness 联调；本阶段没有修改插件协议或官方 Harness 源码。

### 阻塞

- 无实现或构建阻塞；上述项目需要在可观察菜单栏/桌面浮层的实际用户会话中补做运行态验收。

## 6. 第二阶段实施记录（2026-09-13 至 2026-09-14，已完成）

### 已实现

- 插件新增 `tasks` / `subscribeTasks` 能力：读取官方 `sessionController.list()` 的会话摘要，按工作区关联普通会话，排除内部子 Agent，并推送状态变化。
- MoodBall 客户端新增任务摘要模型、当前工作区过滤、任务排序、零/单/多任务显示边界，以及本地已读位置保存。首次基线不会批量制造未读提醒。
- 宠物 UI 按示例收敛为“宠物 → 紧凑操作栏 → 任务卡片”：保留前置笔记按钮，不加入语音按钮；多任务默认折叠，支持详情、更新时间、状态图标、最多四张卡片及内部滚动。
- 点击卡片只查看并标记已读；“继续此会话”单独切换输入目标；存在草稿时拒绝跨会话切换；输入框显示当前目标会话。
- 完成、失败和停止结果会保留在任务卡上，直到查看该项或开始下一轮，不会因宠物动画回到待机而丢失提醒优先级。
- 修复展开输入面板圆角边框的直接阴影：移除 SwiftUI 面板外层阴影，保留材质背景和细边框。

### 已验证

- 官方 Harness 源码当前 Commit `c291e7961a515f6d7af9304e7fd1d257929aef26`、版本 `0.1.5-rc.2`；已确认 `session.list` 是 Host API，`uiWorkspace.openSession(sessionId)` 仅存在于客户端 UI 服务。
- `pnpm typecheck`、`pnpm test:protocol`：3 项协议测试通过，覆盖任务查询、任务订阅推送、普通会话过滤和原有命令协议。
- `swift build -c debug --disable-sandbox`、`swift build -c release --disable-sandbox`：通过。
- `MOODBALL_SKIP_OPEN=1 bash make-app.sh`、`codesign --verify --deep --strict --verbose=2 dist/MoodBall.app`：通过。
- 已将旧版 `/Applications/MoodBall.app` 移入废纸篓，并将新版本原子替换到原路径后启动；安装版签名校验通过，运行进程路径正确。
- 已确认 `dist/MoodBall.app` 与 `/Applications/MoodBall.app` 的主程序 SHA-256 一致：`d1ae3b44a32de8049f7c2f0ad3c7824ea3ba83c0018329e0c0df9e708cc588b5`。
- `git diff --check`：通过。

### 后续运行态验收

- 尚未在用户实际运行的 Harness profile 中重载插件并观察网页端任务、宠物发起任务和跨会话状态变化；本次未重启或修改官方 Harness。
- 尚未完成真实桌面上的多屏/边缘高度、点击穿透、任务卡滚动和中文输入联调截图验收；这些属于安装后的环境验收，不影响本阶段代码与 App 替换完成。
- 官方没有发现可由 Host 插件调用的外部精确会话深链，详情暂提供打开 Harness 与复制会话 ID。

### 阻塞

- 无实现、构建或安装阻塞。

## 7. 第三阶段实施记录（2026-09-16，已实现，待运行态验收）

### 已实现

- 输入框支持直接粘贴剪贴板图片；回形针菜单可启动 macOS 框选截图。框选期间隐藏宠物和附属控件，完成、取消或启动失败后恢复原显示状态。
- 图片进入横向附件区，显示缩略图、名称、大小及失败状态；支持逐项移除和失败重试，也支持纯图片或图片随文字发送。
- 图片草稿保存在权限为 `0700` 的 MoodBall 私有目录，文件权限为 `0600`；移除或 Harness 确认接收后清理对应私有文件，取消截图不改变原有文字和附件。
- 命令协议新增 `imageAttachments` 能力、部署附件限制和动态图像请求上限。Swift 端按 Harness 返回的数量、单张字节与总字节限制预检，不写死默认值。
- 插件把规范 Base64 图片作为官方 `sessionController.prompt` image content block 提交；图片的真实格式、尺寸、像素、模型图像能力及持久化仍由 Harness 官方附件路径校验。MoodBall 不把临时文件路径作为消息内容，也不删除 Harness 持久化附件。
- 超限命令采用有限缓冲并在下一条 NDJSON 恢复；官方错误码和消息通过命令桥返回，断线、超时或服务端拒绝时保留文字与图片草稿及请求 ID，避免静默丢弃或重复提交。

### 已验证

- 当前官方 Harness checkout 为 `0d1f50007f`、版本 `0.1.6-alpha.1`；确认 `sessionController.prompt` 原生接收 image content block，并在创建消息前通过附件服务持久化和校验当前模型能力。官方源码未修改。
- `pnpm typecheck`：通过。
- `pnpm test:protocol`：4 项协议测试通过，覆盖纯文字、纯图片、图文内容适配、附件限制、规范 Base64、官方错误透传、超限输入后的连接恢复和原有任务协议。
- `swift build -c debug --disable-sandbox`、`swift build -c release --disable-sandbox`：通过。
- `MOODBALL_SKIP_OPEN=1 bash make-app.sh`、`codesign --verify --deep --strict --verbose=2 dist/MoodBall.app`：通过。
- `git diff --check`：通过。

### 后续运行态验收

- 尚未在用户实际运行的 Harness profile 中重载本版本插件，因此未声称模型已真实收到图片；需要分别验证纯图片、图文混合、模型不支持图像、上传失败、超限和断线重试。
- 尚未在真实桌面人工验证剪贴板粘贴、框选完成、Esc 取消、屏幕录制权限拒绝、多屏框选和浮层恢复。
- 已将旧版 `/Applications/MoodBall.app` 移入废纸篓，并用第三阶段签名构建产物原子替换后启动；安装版主程序 SHA-256 为 `ef69ca47b171171050f2aff2529d868f5180923ec39feda9ae104299d09ca4f2`，进程路径和签名均已核验。未重启 Harness、未推送或发布。

### 阻塞

- 无代码、构建或协议测试阻塞；真实图片到达与桌面交互仍需在用户运行环境完成最终验收。
